services:
  backend:
    build:
      context: ./backend
    ports:
      - "8080:8080"
    volumes:
      - saves-data:/app/saves
      - ./books:/app/books

  frontend:
    build:
      context: ./frontend
    ports:
      - "4200:80"
    depends_on:
      - backend

volumes:
  saves-data: